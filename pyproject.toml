[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "python-engineering-journey"
version = "0.1.0"
description = "Learning backend engineering with Python"
authors = [{name="Ehsan"}]
dependencies = [
    "requests"
]